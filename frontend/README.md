Frontend (minimal Next.js)

Run:

1. cd frontend
2. npm install
3. npm run dev

This is a minimal scaffold with hardcoded auth for local review. It expects the backend at http://localhost:8000.
Frontend placeholder — Next.js app router with Tailwind + shadcn/ui expected.

Run `npm install` then `npm run dev`.